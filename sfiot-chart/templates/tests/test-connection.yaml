apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sfiot-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sfiot-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sfiot-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
