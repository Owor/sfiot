apiVersion: v1
kind: Service
metadata:
  name: {{ include "sfiot-chart.fullname" . }}
  labels:
    {{- include "sfiot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sfiot-chart.selectorLabels" . | nindent 4 }}
